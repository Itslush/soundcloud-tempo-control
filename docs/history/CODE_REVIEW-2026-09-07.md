# Historical review snapshot

Preserved before consolidation on 2026-09-07. Pending-run statements below describe their original checkpoint, not current status. See [current review](../CODE_REVIEW.md).

# Release code review

Reviewed 2026-09-06; candidate status reconciled 2026-09-07. Scope: userscript, audio adapters, storage, editor, sharing, updates, website, resolver, build and tests.

Current local download: v1.0.0, 480,040 bytes, SHA256 `789a8430f052ed86b1bc3c096a19f3cb5c98b653e8a3aae66fe42849d68355cb`. Independent review approved the startup and queue-fix scope after validating three live reports, their exact source hashes, 115 stored playback measurements and the diagnostic gate. Both pitch modes pass saved/fresh starts, six manual transitions and four completed automatic handoffs with no project warnings or uncaught page errors. Earlier candidates with teardown warnings remain retained as non-acceptance evidence. LOW_RATE_PLAYBACK.md records the reports and remaining limits. The local download update is not an installed-manager update or hosted release.

## Verdict

Current source-only candidate `19c77afa56b34ff3261c498efca036368227dac7166ad299420e284dd1eefacb` uses an esbuild ESM-to-IIFE build, bundled pinned WASM and Mediabunny, an explicit transport lifecycle and guarded native recovery. Its twelve-minute Preserve check covered 69,133,440 output frames without silence, nonfinite samples or frame-index gaps, and passed all 36 clock checkpoints. A separate injected host-method failure recovered to native 0.25× playback. Extreme-rate pitch failures and the cause of the earlier clock shortfall remain unresolved. See ENGINEERING_RELEASE.md for exact reports and diagnostic limits. Current source has not replaced the public download, received a full UI/installed-manager regression or earned higher review scores.

Historical source-only candidate `25448d35` enabled the pinned library's split-computation mode in buffered Preserve. Its targeted tests passed, followed by twelve minutes of sampled playback. Those results do not cover subsequent changes or continuous output. The preceding candidates' results below remain attributed to those candidates.

Source-only candidate `c603c730` includes the independently reproduced EOF replay fixes from `5d9dce39` and bounded reuse of a completed VOD playlist. The replay candidate passed live saved/fresh starts and four actual Repeat-one cycles. The current candidate passes 326 audio tests and a six-minute Preserve run beyond the reproduced playlist-403 failure point. It is not yet the public download. The subsequent one-hour attempt failed its wall-clock tolerance at the 620-second checkpoint despite source progression matching the audio-context clock. LOW_RATE_PLAYBACK.md retains the failed run and its limits. Endurance acceptance is incomplete. None changes the review scores.

The local candidate now exposes 0.025–4× in the userscript, including saved speeds, timelines, backups and shared links. Sliders use a thin rail with an outlined handle on hover or keyboard focus, remaining visible for touch. The expanded ruler retains aligned 0.025× stops. Optional Charcoal and OLED themes preserve SoundCloud's layout. The buffered path passes a short public-track check in both modes on the unmodified build. See LOW_RATE_PLAYBACK.md for evidence and remaining acceptance. Signed-in Edge checks cover an earlier installed build's controls and storage, not the latest low-rate path. Installed playback, native copying, assistive-technology use and sustained profiling still prevent calling the full goal complete. Hosted updates remain a later deployment check.

Earlier independent reviews scored usability **35/40** and technical design **19/20**, totaling **54/60**, for the implemented scope. Those scores have not been increased following the newly reconciled low-rate requirement. They are scoped judgments, not certifications or counts of remaining bugs. No claim of flawless code, inaudible stretching or negligible DSP cost follows from these results.

The low-rate implementation connects the bounded source, PCM cache, clock, Natural and Preserve renderers through the production SoundCloud bridge. Real AAC-LC seek comparisons pass after verified preroll; generated rate transitions, buffer exhaustion and reset isolation also pass. Independent review corrected buffering-gap, ownership and command-cancellation races. Low-rate storage and sharing tests cover boundary rejection before rounding, all five curves and pitch metadata. These results extend the implementation and evidence, not the review scores or claims about subjective quality. LOW_RATE_PLAYBACK.md records resource limits and reproduction commands.

## Review scores

The isolated website preview also passed `verify-site-presentation.py`, `verify-demo-recovery.py`, `verify-interface.py`, `verify-site.py`, `verify-preview-lifecycle.py` and `verify-navigation.py`. Coverage includes five routes, eight interface widths, long titles, reduced motion/forced colours, failure recovery, stale activation and partial graph connections. Ten actual Back/Forward returns restored the cached document across idle, paused and playing modes. The transfer check against this same isolated site passed unchanged limits: 126,788 initial Brotli bytes and 254,412 including every preview crop, with ETag revalidation. Deferred playback code/audio are excluded from those totals. The copied website screenshots and download still represent the older public build; these checks do not update their provenance or prove an installed-manager flow.

Website engine integration: an isolated Astro build at `test-results/site-candidate-20260907` uses freshly compiled engine SHA-256 `953232acca42905cc17101039ceaa3fabe7b0a052ec39c17dd25c170f3523c05`. `verify-demo.py` passed sample playback, WASM mode, pitch-menu keyboard behavior, tempo/seek/reset, file and resolver failures, image failure handling and responsive assertions against that preview. Its old 18-pixel slider-thumb expectation was corrected to the committed 12-pixel CSS/design value; endpoint tolerance remains 0.05 pixels. This is generated-sample preview acceptance, not live remote-track or perceptual verification. Public downloads and `dist/site` were not rebuilt. The build still warns about a deferred chunk above 500 kB.

Correctly attributed reruns completed: `wasm-boundaries-65f8eb16.json` (eight seek/replacement cases plus two detector controls), `embedded-track-65f8eb16.json` and `display-duration-65f8eb16.json` all report PASS with the exact `65f8eb16` candidate hash. The corrected overhead check reports 905,923 script bytes, zero observed idle timer ticks/document scans/animation frames over 3.2 seconds and no such work for 20 unrelated text updates. These are fixture observations, not total CPU measurements or installed-browser acceptance. Earlier wrongly attributed reports remain unsuitable as candidate evidence.

The 19-suite muted browser runner subsequently completed on `65f8eb16`. Its assertions passed, but the WASM-boundary, embedded-track and displayed-duration JSON writers still hardcoded the public-download hash, and overhead reported the public byte count. Those generated reports are not valid candidate provenance. The shared fixture now supplies the same checksum-validated bytes to injection and reporting. Fresh reports are required; do not relabel the existing reports. The WASM-boundary rerun is in progress with output name `wasm-boundaries-65f8eb16.json`.

UI follow-up candidate `65f8eb16b7eaf9d52ed86757534260e843ba726631394546583e9f659df5f003` changes only two settings spacing values from `19c77afa`; compiled worklet bytes are identical. The existing compact-library browser check reproduced five fully visible rows at 320 × 640 on `19c77afa`. Removing eight pixels of excess vertical spacing restored six complete rows without changing the 43-pixel row height. All four layout cases, nine rendered slider-alignment cases and forced-colour checks passed. Desktop/mobile captures were inspected and the scoped design detector reported no findings. The full 19-suite browser run on this follow-up is pending. The twelve-minute audio report remains attributed to `19c77afa`, not relabeled as a new run.

Source regression checkpoint, 2026-09-07: all 520 existing `tests/*.test.cjs` and `tests/*.test.mjs` checks passed, with zero skips or failures; retained JUnit output is `test-results/regression-20260907-19c77afa.xml`. TypeScript `--noEmit` passed. Rebuilding the isolated candidate reproduced `19c77afa56b34ff3261c498efca036368227dac7166ad299420e284dd1eefacb` and the same source-map/worklet hashes. The public artifact remains `789a8430`. These checks cover their asserted source behavior, not installed-manager, visual or perceptual acceptance.

| Usability heuristic | Score |
| ------------------- | ----- |
| System status       | 4/4   |
| Familiar language   | 4/4   |
| User control        | 4/4   |
| Consistency         | 4/4   |
| Error prevention    | 3/4   |
| Recognition         | 4/4   |
| Efficiency          | 3/4   |
| Minimalism          | 3/4   |
| Error recovery      | 3/4   |
| Contextual help     | 3/4   |

Prevention and recovery retain conservative ratings because the visual reviewer did not personally exercise every failure path. Automated recovery tests supply separate evidence. Efficiency has no identified missing accelerator. Mobile editor length and keeping deeper explanations in the guide are design tradeoffs, not established task failures. The review does not recommend adding features merely to increase the total.

| Technical dimension      | Score |
| ------------------------ | ----- |
| Accessibility            | 3/4   |
| Transfer performance     | 4/4   |
| Responsive layout        | 4/4   |
| Theming                  | 4/4   |
| Implementation integrity | 4/4   |

This is not a complete WCAG conformance assessment. Keyboard operation, contrast, target sizes, forced-color emulation and actual 200%/400% browser zoom are tested. Screen-reader output and physical touch remain unverified. The additional zoom evidence does not change the independent review's score by itself.

## Fixed in the quality pass

- Loading waits for usable media metadata before mapping points or enabling sharing. Repeated metadata cannot remap the draft. Preview excerpts cannot export misleading full-track timing.
- Retry resolves a failed stream again. Invalid links, non-JSON upstream errors, local-file failures and superseded requests have explicit recovery paths without silently changing the selected source.
- Fixed speed and Timeline are separate modes. Current rate is distinct from the selected point. Original comparison preserves the draft and listening position. The pitch menu visibly identifies its purpose.
- Undo/redo retains bounded history and groups a drag into one edit. Graph nodes have 44 px hit regions; vertical touch scrolling remains available outside handles. Fractional labels and finer panning support short files.
- The playhead moves without rebuilding the graph. Paused/hidden views avoid animation-frame work. Labels and rate updates are throttled independently.
- Both audio adapters reuse one worklet per media graph rather than recreating it after each pause, seek or mode change. Explicit library resets, serialized updates and latest-rate guards prevent stale work from reactivating. A reusable input gate discards an observed 96 kHz source-boundary pulse before it reaches the processor. This adds one measured engine-latency window, about 120 ms, to restart priming, not continuous tempo changes.
- Menus, disclosures and viewers share reversible motion and reduced-motion handling. Hidden choices are inert. Image placeholders reserve space; failed images can be retried.
- Navigation reflects the current route. Help opens the setup guide; payment links appear only when configured. Stale headroom instructions and repeated promotional copy were removed.
- Saved speeds and timelines share one searchable library. Entries can be enabled, edited or removed with undo. Backup import validates size, tracks, points and preferences before a replacement preview and confirmation. Changed storage invalidates that confirmation; partial writes attempt rollback and report any rollback failure.
- Missing documents return an HTML 404 page; API errors stay JSON. Static delivery validates real paths, supports conditional requests and bounded streaming compression, and preserves the deployment prefix.
- Transfer budgets distinguish initial assets from deferred HLS, audio and full-size images. They test built-server responses, including compression and ETags.
- Website selections retain the exact orange, measuring 5.89:1 normally and 5.08:1 on hover. The footer speed value now inherits the native foreground, without a changed-value background; it and the adjacent × pass 4.5:1 in both fixture themes, including hover. Editor primary actions retain the darker accent surface.
- Authored application comments are absent. Required userscript metadata and upstream licenses remain. Editor CSS has separate declarations. The current candidate bundles the pinned upstream WASM payload through esbuild; the earlier public download uses a different dependency arrangement. Bundling does not eliminate decoding, compilation or DSP costs.
- Slider ticks share one vector coordinate grid instead of separate flex boxes. The userscript marks every 0.025× stop and aligns its rail, fill and outlined thumb with the native travel. Geometry, rendered ring-center alignment, keyboard steps and forced-colour visibility pass in isolated Chromium fixtures. Saved settings use 43 px rows, keep search outside the scrolling list and retain per-setting controls and undo without repeated On/Off labels. Earlier installed-session records predate these UI changes.
- Signed-in Edge testing exposed a separate same-origin embedded track document. A scoped adapter adds its artwork badge and copied-link metadata without another playback controller. Shortlink conversion requires an opted-in, trusted Copy link action in the current track header. Requested and loaded frame URLs are validated; observers, clipboard wrappers, modified links and badges are cleaned up when a frame is replaced or excluded. All 25 independent embedded-view regression groups pass. The installed adapter and settled 0.85× hero badge were confirmed; an immediate pre-render observation was discarded.
- Track covers can reuse artist-avatar images. Fallback recognition now checks the track title, trusted image host, cover container and dimensions instead of excluding every avatars-\* URL. The regression includes visible and hidden responsive covers, title/alt/sizes changes and 11 non-cover cases. Profile/comment avatars remain unmarked. Hidden covers with unresolved complex sizes fail closed until rendered. This fix is local; installed confirmation remains pending.
- A subsequent installed copy check exposed a duration parsing defect before audio loads. SoundCloud places hidden accessibility text beside its visible clock. Sharing and editor initialization now share a strict displayed-clock reader, retain valid media-duration preference and reject unknown durations. Eight independent duration groups cover the exact live markup, copy paths, valid clocks and malformed or missing data. The installed paused editor subsequently opened and copied the correct 394-second track, 0.85× rate and Natural pitch. SoundCloud's own Copy link still omitted metadata after navigation and reload, so native-copy integration remains unresolved. The installed artifact's complete hash is not verified.
- An additional regression reproduced metadata loss when a media element reports zero duration despite a valid visible clock. Sharing and editor initialization now use the same duration selection, with a visible-clock fallback for zero or unavailable media duration. The baseline failed and the corrected build passes all copy paths. This establishes a local defect and fix, not proof that it caused the remaining installed-site failure.

## Subsystem assessment

| Area                  | Assessment                                                                                                                         | Remaining boundary                                                                                                                               |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Player integration    | Event-driven updates, guarded media properties, weak audio references, remount handling and native rail alignment.                 | SoundCloud DOM changes and other extensions can affect prototype interception and routing.                                                       |
| Audio and output      | Pinned Signalsmith dependency, bounded HLS buffers, asynchronous guards, browser fallback and one global gain across pitch modes.  | Synthetic and muted tests establish mechanics, not musical quality or negligible processing cost.                                                |
| Timeline editor       | Validated points, bounded imports, zoom/pan, interpolation, keyboard controls and explicit temporary/saved state.                  | The userscript editor remains a large closure. Further extraction should follow a measured maintenance need.                                     |
| Library and sharing   | Unified inventory, validated backup replacement, removal undo, track matching, pitch metadata and clipboard fallback.              | Storage is browser-local. Services may strip fragments; long timelines produce long links.                                                       |
| Updates               | Deterministic metadata and a once-per-version notice without extra polling.                                                        | Public URLs are unset. Hosted installation/version-increment testing is pending. Older development installs need manual replacement.             |
| Website               | Explicit modes, accessible controls, loading/recovery state, undo, comparison and responsive capture viewing.                      | Real cached Back/Forward restoration passed with generated local audio. Remote HLS restoration and browser cache eviction remain separate cases. |
| Resolver              | Restricted inputs/hosts, no arbitrary proxy, bounded cache/processes/output, deadlines, cancellation and single-flight extraction. | Hosting IPs, regions and upstream changes can affect streams. Proxy users share a socket-IP quota without deployment configuration.              |
| Delivery              | Streaming Brotli/gzip, encoding negotiation, cache validation, prefix handling and transfer budgets.                               | HLS remains a substantial lazy-loaded chunk. Its build warning is retained; it is not initially downloaded.                                      |
| Build and portability | Deterministic output, download equality checks, pinned dependencies and retained licenses.                                         | Local Chromium tests do not establish Firefox, Linux, every manager or a successful GitHub CI run.                                               |

## Verification

- The latest website presentation pass adds a full-width sticky header and a static, varied starfield across the full page without glow. Routine hints and screenshot captions are removed while functional labels and error/loading feedback remain. Header, anchor, decoration and control checks pass at four widths on five routes; custom controls, sharing, recovery, responsive layouts and actual 200%/400% browser zoom also pass. Desktop and mobile visual inspection confirmed the final layout and image viewers. Capture tooling now waits for completed animations and uses viewport shots for sticky-header sections.
- All eleven public screenshots use the explicitly selected Drown music track and the exact `789a8430f052ed86b1bc3c096a19f3cb5c98b653e8a3aae66fe42849d68355cb` build. Saved speed, timeline and recipient actions were exercised through the real UI. The screenshot harness handles SoundCloud's pending autoplay state rather than blindly toggling Play/Pause. Image hashes, unchanged pixels, muted output, recipient save and context cleanup are recorded and checked. This is isolated early injection, not new installed-manager playback acceptance.
- The current build passes 55 release/library/timeline/lifecycle/theme tests, 314 audio groups, 110 host-clock integration tests, 12 completion/diagnostic tests, 33 backend tests and strict website type checking. Commands overlap, so these counts are not an additive total. Seven sliders pass hover, focus, drag, touch emulation, forced-color, reduced-motion and programmatic-fill checks. Numeric controls, inline layouts, tick geometry, responsive interface states and compact-library operations pass their focused browser suites. Theme tests also cover persistence failures, rollback, bounded document attachments and cleanup.
- Independent visual review approved the slider and appearance scope after one correction: the native inner player layer and its secondary buttons now use the same surface as the header. Fresh public legacy-route captures confirm Charcoal and OLED at two widths and native restoration; dark/light fixture crops confirm number/× alignment and the absence of a changed-value patch. These are not new installed-manager or overall score approvals. The only detector warning in this pass was an existing SVG stroke-width transition, not layout animation.
- A live native-to-saved-low transition reproduced a queued `loadstart` cancelling the newly attached buffered owner. The facade now retains ownership for a same-source `loadstart`; `emptied`, explicit source assignments and `load()` still invalidate it. Five new regression groups cover the event race, pending Play/Seek, raw source changes and reload cleanup. The exact rebuilt userscript passes six live row/Next transitions across Natural and Preserve, including direct buffered-to-native-rate handback with Preserve still enabled. Tests use real host controls, inspect Next-up ordering, seek to distinct old positions and check changed source identity, source clocks and sampled output. This is not installed-manager or continuous old-PCM contamination coverage.
- A 240-second public-track session on the earlier `3fa5f846` build passed sampled output and clock checks, split equally between Natural and Preserve at 0.025×. Whole-browser CPU and private memory were substantial. The later partial baseline does not establish their causal attribution. LOW_RATE_PLAYBACK.md records the measurements and limits. This run is not one-hour endurance, continuous audio capture or evidence of negligible overhead.
- The preserved `16441d20` baseline passed all four ordinary-speed conditions at 0.85×, then failed buffered Natural's independent wall-clock check: 40.017 wall seconds advanced the audio context by 35.649 seconds and the source by 0.891233 seconds at 0.025×. Buffered Preserve was not run. Sampled output stayed finite/nonzero and context state was `running`; no cause is assigned to the userscript, browser or device. All attempted browsers closed muted, and the report remains incomplete. Atomic checkpoints retain the completed cases. This is a separate unresolved observation from the host's premature EOF behavior.
- B01, originally verified in artifact `b01ec51097ceb81073cdf7bfeb317f849573d856c1d8166aa31a16518cab6bb0`, retains incoming playback snapshots while suppressing redundant status-view notifications. It changes no DSP, source clock or EOF logic. Its deterministic regressions do not measure live CPU improvement or close the earlier matrix's wall-clock failure; that matrix used `16441d20`.
- An unbundled host-clock candidate (`ef7ce098`) first reached decoded completion before host replacement in separate muted Natural and Preserve tests. The current full userscript (`789a8430`) now performs validated discovery and ownership itself, passing saved/fresh starts, six manual transitions and all four Natural/Preserve automatic handoffs to 0.025× or 0.85×. Outgoing position equals decoded duration before the observed host Pause, with ended/paused true and native playback parked. Identity is checked around the snapshot reads, not after the native Pause call. Ended-event delivery remains unobserved; these are not continuous audible-tail or installed-manager results. Independent review approved this scope without increasing the overall scores.
- The historical `16441d20` automatic queue report remains incomplete: the host replaced each outgoing 0.025× source approximately 0.254 to 0.268 source seconds before decoded completion, equivalent to 10.2 to 10.7 seconds at that rate. Source inspection found rate-blind position extrapolation and a separate HLS duration contract. The integrated host-clock fix closes those four reproduced cases in the new report, not the separate resource-baseline shortfall, repeat, endurance or continuous-output gates.
- The historical `b01ec510` Node run passes build syntax, dependency integrity, metadata-only comments, bounded lines and migration, plus 30 release, library, timeline and worklet-lifecycle unit tests. The separate audio command passes 311 module, ownership, bridge and editor groups; historical report counts are unchanged. A test-only buffered-copy candidate matches an independent reference in 16,430 cases and reproduces the pinned wrapper's original exception. That copy test does not change the production dependency or establish low-rate playback quality.
- npm run typecheck: strict website TypeScript.
- The unmodified 0.025× build passes full-script public playback in Natural and Preserve key, returning to native 0.85×. Two real UI-save/reload cases pass as SoundCloud-requested resumes, which a native-speed control also reproduces. Two separate fresh-context cases use only the saved userscript preferences, cancel pending host autoplay through the UI before any Play call, then start from one explicit Play click. Source clocks remain parked and no native playback warmup is needed. These short, sampled checks are not installed-manager, queue, long-session or perceptual coverage.
- npm run test:server: 33 backend and extractor-process tests, including compression, cache validators, real-path confinement, cancellation and prefixed routes.
- npm run test:transfer: built-server transfer budgets and conditional requests. All font subsets are included conservatively; screenshot crops are reported separately.
- tests/run.py: all 19 muted userscript suites passed on the compact-library and triangle-pointer build, including continuous audio boundaries. The embedded suite has 25 groups; displayed duration has eight. Separate slider checks cover equal tick spacing across widths, CSS scales and device scales. These cover local fixtures; installed native-copy behavior remains unresolved.
- Website suites cover routes and matching downloads, real sample playback, custom controls, loading races and retry, eight responsive widths with long Unicode titles, focus, reduced motion, forced colors and image failure recovery.
- Actual browser zoom passed at 200% and 400% through Chromium's tabs API, without CSS scaling or page-scale emulation. At a fixed 1440 px outer width, the layout viewport changed from 1424 to 712 and 356 px; devicePixelRatio changed from 1 to 2 and 4. Menus, graph keyboard editing, timing fields and Fixed/Timeline controls remained usable without horizontal overflow. No audio loaded.
- Actual Back/Forward navigation passed in Chrome 144 for idle, paused and playing states, including both pitch modes. All five cases restored the same cached document with persisted page events, retained the edited point and playback position, stayed paused on return and resumed nonzero audio. The test used generated local audio with muted output, not remote HLS, and did not synthesize page-transition events. Browser cache eviction can still require a fresh page load.
- Official Violentmonkey 2.48.0 MV3 loaded in an isolated Chrome for Testing 148 profile. The website link opened its real installer; the unmodified script and metadata survived a browser restart. Chromium's separate Allow User Scripts permission was disabled in that isolated profile, so its injection check remained incomplete. The test did not change that permission or simulate GM APIs. Separately, the user's existing Edge installation successfully ran the pre-embedded-fix candidate after manual reinstall. Actual controls, saved-rate reload, removal undo, draft validation and focus restoration were exercised without starting playback. Temporary test data was removed and original preferences restored.
- Both audio adapters passed 48 kHz and 96 kHz lifecycle checks. Older analyser-based silent-position checks can miss intervening pulses and include cached pre-seek history. The additional userscript boundary regression captures every stereo frame from the raw source and routed output in one callback. Eight seek/source-replacement cases at 0.5×/1.5× and 48/96 kHz passed, with zero measured output after the raw source became silent; two contaminated controls were correctly rejected. Captures are bounded, timestamped and hashed. These generated-WAV results do not establish continuous website-adapter coverage, every rate, live SoundCloud or one-hour behavior. Deferred scheduling, paused-mode changes, failures and partial graph connections retain separate regression coverage.
- The final 120.025-second muted website session completed 12 reset/reload/seek cycles with one worklet, versus 19 before reuse. It sampled nonzero audio 109 times and released audio blobs and the cached port on disposal. Whole-page post-GC JS heap rose from 2,455,696 to 2,867,184 bytes. Main-thread TaskDuration was 18.727 seconds, including test actions and GC but excluding DSP CPU. These measurements do not establish total-process memory, native processor destruction or one-hour reliability.
- The latest Brotli transfer check measured 126,788 bytes for initial assets, including every font subset; 254,412 bytes including all refreshed preview crops. Initial JavaScript was 8,796 bytes and CSS 6,684 bytes. Deferred playback dependencies and audio are separate. The build still reports a deferred chunk above 500 kB; passing initial-transfer budgets does not remove that warning.
- tests/verify-live-track.py: the chosen Drown (Sewerslvt Remix) loaded through yt-dlp without credentials as a full 496.503-second HLS stream. Muted 0.75× playback in Natural and WASM modes, nonzero output and a 30-second seek passed. This is not a listening-quality benchmark.
- docs/screenshot-evidence.json records public SoundCloud captures and a receiving-session share flow. These use early injection, not an installed manager.
- Native integration previously measured identical orange accents and 0 px rail-center difference. An idle fixture recorded no timer ticks, document scans or animation frames over 3.2 seconds and unrelated mutations. Neither measures total CPU or heap use.

The independent detector ran once. Its hidden-viewer missing-source warning and stroke-width transition warning were verified as false positives. The full-resolution image loads successfully. Final visual confirmation used headless Chromium after the interactive browser became unavailable; screenshots and computed styles were inspected.

## Before public release

1. Finish installed-manager and startup acceptance for the requested 0.025× playback support. Resolve the actual installed native-copy path and verify fallback-cover artwork. Exercise queue changes, seeks, repeat, background playback, multiple tabs and other audio extensions. Earlier installed Edge controls and storage were tested; signed-in playback is not fully verified.
2. Investigate the earlier buffered wall-clock shortfalls and finish endurance acceptance without weakening assertions. Preserve passed a six-minute run with playlist reuse, but the subsequent one-hour attempt failed at its 620-second clock checkpoint. A separate twelve-minute oscillator control stayed within tolerance. Verify segment failures and renewed-source recovery separately. Test representative long mixes, listen in both pitch modes and measure sustained CPU, worklet lifetime and memory. Extend continuous boundary coverage to the website adapter, buffered transport and live source formats. Short synthetic captures cannot certify every transient or one-hour reliability.
3. Verify screen-reader output, physical touch, Firefox and Linux, plus the remaining Edge playback cases. Actual Chromium browser zoom is covered at 200% and 400%.
4. Configure the public host, HTTPS, proxy limits and stable update URLs. Test full/preview streams, short links, expired URLs and applicable platform terms from that host.
5. Perform a hosted install and real version update. Add the optional PayPal destination only when supplied.
