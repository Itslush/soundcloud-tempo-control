import { BufferSource, CustomPathedSource, EncodedPacketSink, HLS_FORMATS, Input } from 'mediabunny';

export const Mediabunny = Object.freeze({
  BufferSource,
  CustomPathedSource,
  EncodedPacketSink,
  HLS_FORMATS,
  Input,
});
